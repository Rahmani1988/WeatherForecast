package com.common.model.constants

object WearSyncConfig {
    const val WEATHER_PATH = "/current_weather"

    object Keys {
        const val CITY = "city"
        const val SUMMARY = "summary"
        const val TIMESTAMP = "timestamp"
    }
}